<div class="container">
  <div class="row">
    <div class="col-sm-10 px-sm-0 mx auto">
      <h2 class="font-weight-bold mt-5 mb-5"><i class="fas fa-edit"></i>商品編集</h3>
        <%= form_with model: [:admin, @item], local: true do |f| %>
          <%= render 'form_item', f: f %>
          <%= f.submit '変更を保存', class: "btn btn-success btn-md my-3 mx-5" %>
        <% end %>
    </div>
  </div>
</div>
